<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="false">

    <!-- 支持从 Spring 属性注入日志目录（优先使用 logging.file.path） -->
    <springProperty name="log.path" source="logging.file.path"/>
    <!-- 日志目录默认值（使用时写成 ${log.path:-logs}） -->
    <property name="file.charset" value="UTF-8"/>
    <property name="log.pattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{traceId:-}] %msg %c [%F:%L]%n"/>

    <!-- 控制台输出：INFO 及以上 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>${log.pattern}</pattern>
            <charset>${file.charset}</charset>
        </encoder>
    </appender>

    <!-- file_info: 接受 INFO/WARN，也打印 ERROR 日志 -->
    <appender name="file_info" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path:-logs}/modbus-simulator.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path:-logs}/modbus-simulator-%d{yyyy-MM-dd}.log.zip</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
            <charset>${file.charset}</charset>
        </encoder>
    </appender>

    <!-- file_error: 只接受 ERROR -->
    <appender name="file_error" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path:-logs}/modbus-simulator-error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path:-logs}/modbus-simulator-error-%d{yyyy-MM-dd}.log.zip</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
            <charset>${file.charset}</charset>
        </encoder>
    </appender>

    <!-- 异步包装磁盘写入，降低主线程阻塞 -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>5000</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="file_info"/>
        <appender-ref ref="file_error"/>
    </appender>

    <root level="INFO">
        <appender-ref ref="console"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>
</configuration>
